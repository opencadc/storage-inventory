## deployable containers have a semantic and build tag
# semantic version tag: major.minor
# build version tag: timestamp
REPOBASE="images.opencadc.org/storage-inventory"

VER=0.3.0
TAGS="${VER} ${VER}-$(date --utc +"%Y%m%dT%H%M%S")"
unset VER
