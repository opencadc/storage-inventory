# Compatibility API 

The modules in the directory implement compatibilitry APIs (usually backwards compatbility)
for clients that do not use the standard storage-inventory APIs.

These are only needed and used by CADC and probably not of interest to anyone else.

